# CPPLINT.cfg
set noparent
filter=-legal/copyright
filter=-build/include_subdir
# clang-format (the formatting authority for this repo) wraps long template
# declarations onto indented continuation lines, which this cpplint check
# misreads as namespace block indentation.
filter=-whitespace/indent_namespace
