module(
    name = "automemoize",
    version = "1.0.0",
)

# Dependencies of the library itself, inherited by consumers.
bazel_dep(name = "abseil-cpp", version = "20260107.1", repo_name = "absl")
bazel_dep(name = "rules_cc", version = "0.2.17")

# Test and tooling dependencies, invisible to consumers.
bazel_dep(name = "buildifier_prebuilt", version = "8.5.1", dev_dependency = True)
bazel_dep(name = "google_benchmark", version = "1.9.5", dev_dependency = True)
bazel_dep(name = "googletest", version = "1.17.0.bcr.2", dev_dependency = True)
